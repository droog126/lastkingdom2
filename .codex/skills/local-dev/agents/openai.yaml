interface:
  display_name: "LK2 Local Dev"
  short_description: "Fallback workflow for ordinary repository changes"
  default_prompt: "Use $local-dev for an ordinary repository change with focused validation."
