# 循环工件

拥有健康、断言、回归、清单、摘要和决策模板的模式和写入。

- 保持工件名称和含义与当前文档和技能同步。
- 尽可能使用稳定的、可版本化的结构和确定性排序。
- 不要将运行时输出放在仓库根目录。

## 当前锚点

- 自然工件写入迭代目录的 `nature.json`，当前 `NATURE_ARTIFACT_SCHEMA` 为 `1`；断言按 id 排序后再计算 verdict。
- `FAIL` 只由失败的硬断言产生，其他失败产生 `PARTIAL`；不要在工件层悄悄放宽断言语义。
