# 回归场景

拥有已修复 bug 和受保护行为的最小复现。

- 仅当保护稳定的运行时接缝且单元测试无法更好地覆盖时才添加场景。
- 说明回归条件和预期的失败信号。
- 不要将此目录用作通用场景存档。

## 当前锚点

- `nature_no_nan.json` 保护固定 seed 下的确定性、无 NaN/负值和错误日志预算；它不默认要求视觉成功。
- 可由单元测试稳定覆盖的规则优先写测试，只有运行时接缝或工件契约才放回归场景。
