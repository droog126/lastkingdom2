# 循环运行器

拥有进程生命周期、命令构建、超时、关闭以及离线/在线运行编排。

- 不要在此处嵌入模拟断言。
- 归档标准输出、标准错误、退出状态和启动配置。
- 确保失败的子进程不会被误认为成功的观察。

## 当前锚点

- 运行器输出必须能区分启动失败、超时、非零退出和成功但证据缺失；下游观察器不能把缺证据当成 PASS。
- 进程参数和默认模式由 `loop_cmd`/`xtask` 统一构造，新增流程不要在场景或断言中自行启动子进程。
