interface:
  display_name: "Game Logic Audit"
  short_description: "Audit staged and unstaged logic changes read-only"
  default_prompt: "Use $game-logic-audit to audit staged and unstaged game-logic changes, authority boundaries, remote-input safety, and validation evidence."
