# 持久化

拥有保存/加载、快照、事件日志、模式版本和恢复。

- 保留恢复世界所需的确定性状态。
- 不要序列化客户端表现实体或 GPU 资产。
- 使用原子化、版本化的写入，并在权威恢复前验证加载的不变量。

## 当前锚点

- 当前实现是 `NatureSave<T>` 的版本化内存 DTO，`CURRENT_SCHEMA_VERSION` 为 `1`；插件只暂存最新快照。
- 这里尚未提供磁盘存档或完整恢复流程；不要把 `LatestNatureSave` 描述成已完成的持久化系统。
