# 服务器网络

拥有连接生命周期、传输配置、消息入口和消息出口。

- 将网络消息转换为经过验证的权威输入。
- 将模拟规则和持久化策略保留在此目录之外。
- 将核心中的协议类型视为线路契约。

## 当前锚点

- `decode_world_input` 只做 tick 单调性验证和 `WorldInput` 转换；不要在网络层推进生态或修改资源。
- 网络消息到权威输入的错误应可观察，不能静默丢弃后继续伪造成功状态。
