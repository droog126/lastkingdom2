# 客户端同步

拥有快照缓冲、插值、实体标识映射和事件应用。

- 在线和离线模式使用相同的表现输入。
- 不要将预测转换为权威生态状态。
- 明确处理延迟、重复、缺失和乱序更新。
- 将传输设置保留在此目录之外，将视觉实体构建保留在表现中。

与此文件相关的规则：

规则名称： f:\rustProject\lastkingdom2\crates\client\src\synchronization\AGENTS.md

```
# 客户端同步

拥有快照缓冲、插值、实体标识映射和事件应用。

- 在线和离线模式使用相同的表现输入。
- 不要将预测转换为权威生态状态。
- 明确处理延迟、重复、缺失和乱序更新。
- 将传输设置保留在此目录之外，将视觉实体构建保留在表现中。


```
