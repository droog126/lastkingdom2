# 万国起源 · 完整架构总纲
## 三套架构文档索引与快速导航

---

## 文档结构

本项目架构分为**三套独立但关联**的文档，分别服务于不同的设计层面：

| 文档 | 文件名 | 关注层面 | 读者 |
|------|--------|---------|------|
| **玩法架构** | `01_Play_Architecture.md` | "做什么" | 策划、制作人 |
| **服务器/客户端架构** | `02_Server_Client_Architecture.md` | "怎么连" | 后端、网络工程师 |
| **系统内容架构** | `03_System_Content_Architecture.md` | "怎么算" | 客户端、系统工程师 |

---

## 核心设计原则

### 1. 世界自运行
服务端启动后，世界按照物理法则和生态规则**自行演化**，无需客户端连接。
- 植物生长、动物繁殖、天气变化持续进行
- 国家 AI 自动管理（如果没有玩家国王）
- 经济系统自我调节

### 2. 资源守恒
世界总质量/能量为**常量**，任何变化都是形态转化。
- 没有"刷新"，只有"转化"
- 所有资源变动可追溯、可审计
- 玩家行为受物理法则约束

### 3. 涌现式玩法
设计者只定义**底层规则**，玩法由规则自发涌现。
- 过度放牧 → 草原退化 → 沙尘暴
- 筑坝截流 → 上游洪水 / 下游干旱
- 文明崛起 → 新技术涌现

### 4. 确定性模拟
给定相同的初始种子 + 输入序列，任何时刻的世界状态都可以**精确复现**。
- 支持时间倒带（查看历史）
- 支持平行分支（实验不同选择）
- 支持跨平台一致性验证

### 5. 多时间流速
不同区域/维度可以有不同的时间流速。
- 农场 10x 加速，主城 1x 正常
- 以太界 0.5x + 重力扭曲
- 分支时间线独立演化

---

## 架构关系图

```
┌─────────────────────────────────────────────────────────────┐
│                      玩法架构层                               │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │
│  │ 生态系统 │ │ 国家系统 │ │ 经济系统 │ │ 魔法科技 │         │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘         │
│       └────────────┴──────────┴────────────┘               │
│                    "做什么"                                   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   系统内容架构层                               │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │
│  │ 资源转化 │ │ 物理模拟 │ │ 涌现规则 │ │ 记忆约束 │         │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘         │
│       └────────────┴──────────┴────────────┘               │
│                    "怎么算"                                   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  服务器/客户端架构层                            │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │
│  │ Tick引擎 │ │ 网络同步 │ │ 状态快照 │ │ 持久化   │         │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘         │
│       └────────────┴──────────┴────────────┘               │
│                    "怎么连"                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 关键技术选型

| 层面 | 技术 | 理由 |
|------|------|------|
| **游戏引擎** | Bevy (Rust) | ECS 原生、性能、确定性 |
| **网络库** | lightyear | Bevy 原生、客户端预测、回滚 |
| **序列化** | bincode | 零拷贝、跨平台一致 |
| **数据库** | sled | 嵌入式、高性能、适合游戏 |
| **压缩** | zstd | 高压缩比、快速解压 |
| **容器化** | Docker | 部署标准化、水平扩展 |

---

## 快速开始

### 开发环境搭建

```bash
# 1. 安装 Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 2. 克隆项目
git clone https://github.com/your-org/wanguo.git
cd wanguo

# 3. 构建
cargo build --release

# 4. 启动服务端
cargo run --bin wanguo-server -- --dimension overworld

# 5. 启动客户端
cargo run --bin wanguo-client -- --connect localhost:7777
```

### 项目结构

```
wanguo/
├── Cargo.toml              # Workspace 定义
├── shared/                 # 共享代码（协议 + ECS 组件 + 系统逻辑）
│   ├── src/
│   │   ├── components/     # ECS 组件定义
│   │   ├── systems/        # 共享系统逻辑
│   │   ├── resources/      # 全局资源池等
│   │   ├── net/            # 网络协议
│   │   └── world/          # 世界生成、区块管理
│   └── Cargo.toml
├── server/                 # Headless 服务端
│   ├── src/
│   │   ├── main.rs
│   │   ├── tick_engine.rs  # Tick 调度
│   │   ├── persistence.rs  # 异步持久化
│   │   └── authority.rs    # 服务端权威验证
│   └── Cargo.toml
├── client/                 # 渲染客户端
│   ├── src/
│   │   ├── main.rs
│   │   ├── render/         # 体素渲染
│   │   ├── prediction.rs   # 客户端预测
│   │   ├── rewind.rs       # 时间倒带
│   │   └── ui/             # 游戏 UI
│   └── Cargo.toml
└── docs/                   # 架构文档
    ├── 01_Play_Architecture.md
    ├── 02_Server_Client_Architecture.md
    └── 03_System_Content_Architecture.md
```

---

## 里程碑

| 阶段 | 周期 | 目标 | 可玩性 |
|------|------|------|--------|
| **MVP** | 4 周 | 基础世界 + 玩家移动 + 方块操作 + 网络连接 | 可探索 |
| **Alpha** | 8 周 | + 生态系统 + 国家系统 + 经济系统 | 可建国 |
| **Beta** | 12 周 | + 完整职业 + 外交战争 + 终局内容 | 可国战 |
| **Release** | 16 周 | + 王冠世界 + 社交系统 + 运营工具 | 可长线 |

---

## 联系方式

- 技术讨论：#wanguo-dev (Discord)
- 设计文档：Notion / Lark
- 代码仓库：GitHub
- CI/CD：GitHub Actions
