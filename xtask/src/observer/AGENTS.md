# 循环观察器

拥有状态、事件、日志、截图、大气、水文和生态证据的解析和规范化。

- 对运行中的世界保持只读。
- 明确记录缺失或过时的证据。
- 将裁决策略保留在断言中，将工件格式保留在工件中。

## 当前锚点

- 自然观察器按兼容路径读取 `nature`、`nature_snapshot`、`eco_cycle` 和旧平铺字段，并规范化排序事件与日志错误。
- 当当前 tick 不高于前一观察或缺少 tick 时标记 `stale`；观察器不负责决定 PASS/FAIL。
