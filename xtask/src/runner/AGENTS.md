# 循环运行器

拥有进程生命周期、命令构建、超时、关闭以及离线/在线运行编排。

- 不要在此处嵌入模拟断言。
- 归档标准输出、标准错误、退出状态和启动配置。
- 确保失败的子进程不会被误认为成功的观察。

与此文件相关的规则：

规则名称： f:\rustProject\lastkingdom2\xtask\src\runner\AGENTS.md

```
# 循环运行器

拥有进程生命周期、命令构建、超时、关闭以及离线/在线运行编排。

- 不要在此处嵌入模拟断言。
- 归档标准输出、标准错误、退出状态和启动配置。
- 确保失败的子进程不会被误认为成功的观察。


```
