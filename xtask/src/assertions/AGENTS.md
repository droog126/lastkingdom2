# 循环断言

拥有确定性、守恒性、种群、水循环、运行时以及状态/视觉一致性检查。

- 优先使用机器可读的状态断言，而非图像推断。
- 保持阈值命名明确、有正当理由、感知场景且经过回归测试。
- 视觉成功绝不能掩盖模拟失败。

## 当前锚点

- 自然断言默认要求确定性、因果进展、表现一致性和零错误日志；场景可显式关闭某一项。
- 缺字段、非有限/负值、过期快照和重复运行不一致属于硬失败；部分因果进展可降级为 `PARTIAL`。
