# 开发与闭环控制

此模块负责持久的跨平台工作流自动化和观察-决策-执行契约。

- 启动和监督离线或在线运行，收集工件，评估断言，并报告健康状况。
- 将核心/服务器状态视为事实证据，将截图视为表现证据。
- 将工作流逻辑保留在 Rust 中；`justfile` 保持为简短别名。
- 除非当前文档和验证一起更新，否则保留现有的工件契约。

与此文件相关的规则：

规则名称： f:\rustProject\lastkingdom2\xtask\src\AGENTS.md

```
# 开发与闭环控制

此模块负责持久的跨平台工作流自动化和观察-决策-执行契约。

- 启动和监督离线或在线运行，收集工件，评估断言，并报告健康状况。
- 将核心/服务器状态视为事实证据，将截图视为表现证据。
- 将工作流逻辑保留在 Rust 中；`justfile` 保持为简短别名。
- 除非当前文档和验证一起更新，否则保留现有的工件契约。


```
