# 客户端

此模块负责表现层、玩家输入、相机、HUD、同步、截图和本地用户体验。

- 渲染权威快照和事件；不要自行生成云、雨、植物、动物、出生、死亡或资源变化。
- 离线模式可以托管一个权威适配器，但必须调用与服务器相同的核心模拟步骤。
- 逐步将职责从 `main.rs`、`render` 和 `pretty` 中迁移出来，避免重复行为。
- 在移动后和随时间推移验证可见变化，而不仅仅是在生成时验证。
